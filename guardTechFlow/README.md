# Legacy Archive

This folder contains historical prototype serverless functions kept for reference only.

Current production-target architecture is the local monorepo:

- backend: Fastify + TypeScript + MongoDB + Redis + RabbitMQ
- frontend: React + Vite + Tailwind

Use the root-level folders for active development.
